type Movie {
  uuid: UUID!
  title: String!
  rating: Int!
  genres: [Genre!]!
  cast: [Cast!]!
  releaseDate: Date!
  createdAt: DateTime!
  updatedAt: DateTime!
}

input MovieInput {
  uuid: UUID!
}

type Query {
  movies: [Movie!]!
  movie(input: MovieInput!): Movie!
}
