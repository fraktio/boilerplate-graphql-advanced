name: CI
on: [push]

jobs:
  ci:
    runs-on: ubuntu-latest
    container: node:15

    services:
      postgres:
        image: postgres
        ports:
          - 54321:5432
        env:
          POSTGRES_PASSWORD: graphql-boilerplate-test
          POSTGRES_USER: graphql-boilerplate-test
          POSTGRES_DB: graphql-boilerplate-test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
    - uses: actions/checkout@v1

    - name: Install dependencies
      run: yarn install --frozen-lockfile

    - name: Generate codegen
      run: yarn codegen
    
    - name: Rerun typecheck for generated contents
      run: yarn typescript-check --noEmit
    
    - name: Lint
      run: yarn lint

    - name: Schema validation
      run: yarn validate-schema

    - name: Tests
      run: yarn test
